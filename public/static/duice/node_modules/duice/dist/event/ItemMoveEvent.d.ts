import { Event } from "./Event";
export declare class ItemMoveEvent extends Event {
    fromIndex: number;
    toIndex: number;
    constructor(source: any, fromIndex: number, toIndex: number);
    getFromIndex(): number;
    getToIndex(): number;
}
