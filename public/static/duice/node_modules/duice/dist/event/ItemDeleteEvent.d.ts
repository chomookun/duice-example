import { Event } from "./Event";
export declare class ItemDeleteEvent extends Event {
    index: number;
    items: object[];
    /**
     * Constructor
     * @param source source
     * @param index deleted item index
     * @param items delete items
     */
    constructor(source: any, index: number, items: object[]);
    /**
     * Gets deleted item index
     */
    getIndex(): number;
    /**
     * Gets deleted items
     */
    getItems(): object[];
}
