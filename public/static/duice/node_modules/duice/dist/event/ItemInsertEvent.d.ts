import { Event } from "./Event";
export declare class ItemInsertEvent extends Event {
    index: number;
    items: object[];
    constructor(source: any, index: number, items: object[]);
    getIndex(): number;
    getItems(): object[];
}
